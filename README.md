# wardrobe-parser-service
Internal parser service module for Wardrobe Parser Platform. See main repo.

Integration tests are executed from the repository root:

```bash
docker compose --profile test run --rm integration-tests
```
